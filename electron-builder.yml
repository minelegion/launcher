productName: "MineLegion"
appId: "minelegion.launcher"

asar: true

directories:
  output: dist
  buildResources: resources

files:
  - from: .
    filter:
      - package.json
      - app

win:
  icon: "/renderer/public/images/favicon.ico"
  target:
    - "zip"
    - "nsis"

linux:
  category: Development
  target:
    - "AppImage"
    - "deb"
    - "rpm"

nsis:
  oneClick: false
  perMachine: false

publish:
  provider: "github"